---
# Copy this file to secrets.yml, fill in the values, then encrypt it:
#   ansible-vault encrypt secrets.yml
tskey: "tskey-auth-XXXXXXXXXXXX"
rootpassword: "changeme"
ssh_public_keys:
  - "ssh-ed25519 AAAA... user@host"
  - "ssh-rsa AAAA... user@host"
